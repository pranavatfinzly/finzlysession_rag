# Pinned to the exact versions verified to work together on Python 3.14 (see README
# "Environment" section). sentence-transformers pulls in a matching CPU-only torch
# build automatically as a transitive dependency — do NOT add torch/torchvision here,
# this app is text-only (no image models), and torchvision is not a runtime requirement.
streamlit==1.63.0
chromadb==1.5.9
sentence-transformers==6.0.1
pypdf==6.18.0
requests==2.34.2
python-dotenv==1.2.2

# Corporate machines doing TLS inspection with an internal root CA break
# certifi-based SSL verification for huggingface_hub's model-download requests
# (see README) — huggingface_hub's httpx client builds its SSL context from
# certifi's bundled CAs, which never include an internal corporate root CA.
# truststore replaces that with the OS's own trust store (Windows/macOS/Linux),
# which already trusts the corporate CA. Injected explicitly in llm.py.
truststore==0.10.4
